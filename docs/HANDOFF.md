# 세션 인수인계 (원격 동기용)

> 집 PC의 `C:\박관용\CLAUDE\CLAUDE_CODE_TASKS.md`는 로컬 전용이라 다른 PC에서 못 봄.
> **PC를 옮겨 작업할 때는 이 파일**을 진입점으로 사용한다. (git pull 후 여기부터 읽기)
>
> ## ▶▶ 2026-07-31 이후 작업은 **`docs/NEXT_SESSION.md` 를 먼저 읽는다** ◀◀
> 착수 순서(A→B→C→D)·보류 항목·주의사항이 거기에 정리돼 있다.
> 이 파일은 날짜별 상세 기록이고, NEXT_SESSION.md가 실행 지시서다.

---

## 2026-08-01 (토) 새벽 — 집 PC 세션 연장전: 수습·형수 방향·대시보드 (마감 보고는 NEXT_SESSION §0)

- 깨진 링크 47건 전건 복구 → B그룹 웨이브1 5건 삭제+재발행(승인) → 재스캔(17건 발견)
  → 16건 재연결 배치(1건 후보 부족 보류). 도구: run_bgroup_wave/scan_broken_links/
  build_relink_plan/fix_related_links 사이클 완성.
- 종료 제도 게이트 오탐 수정(미래 시행 연도 통과 — 가상자산 과세 실사고).
- 형수 전수 점검(73건, 결함 4)+방향 T1~T8(TECH_DIRECTION_2026-07-31.md).
  트래픽 재비교로 '정반대' 철회(메인 노출 단계 차이 — 사용자 가설 적중).
  경쟁 5블로그 실측(이미지 4배·색 33배·후킹 제목 역효과). 수익화 9/11 최종.
- 토스 투자기록 전말 규명(EC2 크론 draft 기본 — 2주차 draft 대기, WP 메뉴 추가).
- 대시보드: 홈 심플화+/full 보존+공통 네비+전 채널 통계 버튼(Koko 원격 설치)+
  트래픽 전 채널 요약·추이+겜코드·플랜트로그·유진 랜딩.
- 요즘경제 소스 3→63개(검증 게이트)+한은 RSS+M1 키워드 수리. 유입채널 일별 누적 신설.

---

## 2026-07-31 (금) 밤 — 집 PC 세션: 착수 순서 A~D 전부 구현 완료

### 완료(전부 push — 상세는 NEXT_SESSION.md §1)
- **A. A/S 4필드**(`08875aa`): `generator/as_fields.py` + info/gov/stock 이력 기록.
  수치 본문 대조를 코드로 강제 — claim에 없는 수치면 전체 폐기, 쌍 단위는 쌍만 제거.
  오프라인 21케이스 + 라이브 추출 1회 검증.
- **통계 전수 수집**(`9e26f7f`, 인계 §6): 추측 경로 프로브가 41전패한 원인은
  ①`serviceId`(→`service`) ②경로 추측. **크리에이터 어드바이저 번들 JS에서 실제
  경로를 추출**해 해결(`_probe_stats_bundle.py`). 재방문율(주간 창 월~일 필수)·
  체류시간·성별연령(코드 07=40-44세)·기기·시간대(metric=cv, date 단위)·국가·
  이웃분포(actionType=add/delete — cancel 아님)·급상승·실시간 히스토그램까지 수집.
  인플루언서 전용(infl-inflow-rate 403)·서버 500 2종은 미지원으로 기록.
- **B. 골격 게이트**(`e749746`): `generator/skeleton_gate.py` — 메르 실측 상수를
  게이트로. 요즘경제 전면(소제목 0·헤더카드 125자 뒤·1문장=1문단·마감 3문장 코멘트
  게이트를 표절·호흡 되먹임 루프에 결합), 아침브리핑은 첫블록 이미지 금지만
  (무지개로그 실험 정체성 유지), 기존 정보성 트랙 미적용(§2 '줄이지 말 것').
  오프라인 20케이스.
- **C. 오늘의 계산**(`ae6d5f3`): `generator/calc_slot.py` — 산식 5종(금1돈·기름1리터·
  비트코인 원화·기술주 스프레드·1만달러 환전차액) 화이트리스트+일자 로테이션+결손
  폴백. 계산·문장 전부 코드(LLM 0회), 입력·산식·출처·출력 로그. 실데이터 13지표 검증.
- **D. M1 연대기 1단계**(`28008f1`): `generator/fact_chronicle.py` — 4-튜플 적재→
  URL·금지목록·날짜 게이트→기계 번호→LLM 튜플→문장 변환→숫자 대조(실패 항목
  삭제+번호 재부여)→하한 15항. 1차 소스 DART 오늘공시 RSS(키 불요, 200 실측).
  econ_digest_post는 M1 우선, 미달 시 서사형 폴백. 부처 RSS는 전 경로 404/빈 응답
  — 재조사 필요.

### 독자층 실측(신규 통계)
40-44세 170 · 45-49세 155 · 35-39세 100(여성 우위) — "2030 생활 재테크" 가정 반증.
재방문율 1.69% · 모바일 75% · 피크 13시. 전략 반영은 사용자 논의 후.

### 다음 세션
NEXT_SESSION.md §2 — ①실험 트랙 라이브 검증 ②깨진 링크 46건 재개 ③B그룹 63건
④M1 소스 확장. PowerShell git commit은 `-F 파일` 방식(§5-6 신규 주의사항).

---

## 2026-07-31 (금) 종일 — 회사 PC 세션 마감 ★★실행 지시서는 `docs/NEXT_SESSION.md`

### 오후 추가분 — 벤치마크 재분석으로 방향 확정

**요즘경제 트랙이 신설 이후 한 번도 발행에 성공한 적이 없었다.** 워크플로는 매일 초록불인데
`econ_digest_state`의 posted가 빈 채였다. 후보 12건을 모아놓고 **1건만 시도**한 뒤 표절
게이트에 걸리면 포기하는 구조였다(`788db72` 소스 폴백으로 수리 → 첫 발행 성공).

**문체를 서사형으로 전환**(`a596555`). 사용자 평가 "소재만 빌려왔다기엔 현지언니 스타일과
다르고, 글을 가져왔다기엔 내용이 너무 다르다". 원본 대조 실측이 원인을 짚었다 —
메르 2,233자·117줄·**평균 17자/줄** ↔ 발행본 2,673자·48줄·**평균 53자/줄**.
몰입감 차이의 실체는 호흡이었다. 실험 카테고리에는 현지언니 페르소나를 쓰지 않기로 확정.

**벤치마크 심층 분석 3건 실행**(에이전트 24개, 원본 `docs/benchmark/`):
- `analysis_traffic_gap.json` — 메르 10만 vs 우리 0의 격차 원인
- `analysis_hyungsutech.json` — 형수테크 폴드8 8~12건 편중 원인
- `analysis_mer_templates.json` — 메르 본문 24건 HTML 전수 구조 해부

핵심 결론은 NEXT_SESSION.md §2에 정리. 요약하면 **연재 가설은 폐기**(단발 72%가 성과 우위),
**1개 골격 + 2 레지스터**로 수렴, **소제목 0/24·볼드 1개·강조는 색만**, 한줄 코멘트 24/24.

**분석 과정에서 내가 두 번 틀렸다**(같은 실수 반복 금지):
1. 검증자가 기각한 제안을 결론으로 가져가 연재 설계를 헛돌렸다.
2. 제목 몇 개만 보고 "연재 구조"라 단정했다가 세어보니 24%였다.

**아침증시 브리핑 설계 오류 검토**(사용자 지적): 벤치마크 문서(`docs/BENCHMARK_무지개로그.md`)
P4에 "슬롯 이원화·포트폴리오 6:2:1.5:0.5"를 적어놓고 구현은 아침 브리핑 하나만 떼어
별도 카테고리로 고정했다. 문서 개요에 "댓글 반응은 꿀팁이 최고"까지 측정해놓고도 그랬다.
적용 후보도 "1순위 stock_post 트랙"이라 적어놓고 새 트랙을 만들었다 —
가독성 개선이 기존 300건 대신 신규 2건에만 적용됐다.
게다가 무지개로그는 **TODAY 48로 우리보다 5배 작은 블로그**였고, 대상 검증을 안 했다.

### 오전 — 겜코드 가짜 코드 사고 외

## 2026-07-31 (금) 오전 — 회사 PC 세션

### 🔴 겜코드 쿠폰 코드 대부분이 가짜였다 (사용자 제보에서 발견)
표의 '최초 확인'이 `1754552956` 같은 숫자로 보인다는 제보로 파고들었더니, 표시 문제
위에 훨씬 큰 게 있었다. **세나키 허브의 11개 코드가 전부 쿠폰이 아니라 '추천 덱 편성'
글의 덱 코드**였고(= 100% 가짜), **나혼렙 56개 중 31개는 해외 공지 대문자 제목에서
딸려온 일반 영단어**(OVERSEER·FORBIDDEN·ARTIFACT…)였다. 커밋 `2909d68`.

- 원인 ①: `regDate`가 ISO가 아니라 epoch인데 `str(v)[:10]`이 숫자를 그대로 통과.
  → `_as_date()`(초·밀리초·ISO 정규화) + `_load_state` 자가치유.
- 원인 ②: `\b([A-Z][A-Z0-9]{5,19})\b`가 점으로 이어진 덱 코드
  (`JV.KB...KIKTKXKAKWJZ.KZKEKK..#W`)를 조각내 매칭. → 점·샵 인접 배제 + '덱 편성' 글 제외.
  ※ 제목 게이트에 티어표·시작덱까지 넣었다가 쿠폰 실린 공략글이 통째로 날아가 좁게 되돌림.
- 원인 ③: `blob2 = title + text`라 대문자 제목이 코드 광산이 됨. → **제목은 추출 제외**.
- 원인 ④: LLM 검증이 실패하면 후보를 그대로 통과시키던 폴백. → **fail-closed**로 전환.
- 데이터 정리: 나혼렙 56→23, 세나키 11→0. 0건이면 빈 표 대신 "확인된 코드 없음" 문구.
- `tests/test_gamcode_extract.py`(오프라인 6종) 추가 + 발행 전 워크플로에서 실행.
- 표 모바일: 4열이 390px에서 터져 상태값이 한 글자씩 세로로 쪼개지던 것 → ≤600px에서
  카드형으로 세움. 로컬 렌더로 390/1000px 가로 넘침 0 실측.
- **🔜 남은 조치**: 라이브 반영은 겜코드 크론(13:00/21:00 KST)이 함. 수동 dispatch는
  권한 차단으로 못 돌렸으니 **크론 후 두 허브 육안 확인** 필요.
- **⚠️ 미결정(사용자 판단 필요)**: '상태' 열이 `last_seen` 기준이라 **항상 ✅ 유효 추정**이
  뜬다. last_seen은 '아직 포럼 아카이브에 있음'일 뿐이라 ❌만료 분기가 사실상 죽은 코드다.
  first_seen(원글 작성일) 기준으로 바꾸면 정확해지지만 오래된 코드가 대량 만료 표시된다.

### 완료(전부 push)
- **워크플로 타임아웃 전면 지정**(hyunji `c67a793`): 07-30에 info_post 4h24m·stock_post
  4h59m **실제 실행** 후 취소(정상은 10~20분). 20개 중 16개에 타임아웃이 없어 기본 360분을
  태웠다. 네이버+랜덤딜레이 45분 / 브라우저 30분 / API 20분. **tech_post는 30→45 상향**
  (딜레이15+셋업5+실행20이면 30분을 넘겨 정상 런이 죽는다).
  ※ 런 목록의 3~4시간은 **큐 대기**라 실행시간이 아니다. timeout-minutes는 큐를 안 센다.
- **발행 전체 데드라인**(hyunji `2946fe0`, 인계 6번): Playwright는 `keyboard.type/press`에
  타임아웃이 **없다**(입력 디스패치라 actionability 대기 없음) — 렌더러가 멎으면 무한 대기.
  `post_to_naver_blog`를 `asyncio.wait_for`로 감쌈(`NAVER_POST_TIMEOUT_SEC`, 기본 1200초)
  + 본문은 `_type_guarded`로 줄 단위 데드라인(어느 줄에서 멎었는지 로그에 남김).
  **자동 재시도는 안 넣었다** — 어느 단계에서 멈췄는지 몰라 중복 발행이 될 수 있다.
- **이력 미커밋 ::warning 오탐 보정**(인계 7번): '스테이징 0건'이면 무조건 울려 정상 스킵
  런에서도 경고가 났다. → add 뒤 **남은 미스테이징 변경**만 보고, 범위는 `HIST_FILES`로
  한정(스크린샷·쿠키까지 세면 새 오탐). 3경우 로컬 실측.
- **법적 페이지 생성기**(niche `5be7530`, 인계 4번): `common/legal_pages.py` +
  `upsert_page`. 소개·개인정보처리방침·연락처 3종. 애드센스는 **제3자 광고 쿠키 고지**가
  없으면 반려라 Google 광고정책·광고설정·aboutads.info 옵트아웃을 명시했다.
  **🔜 워크플로는 크론 없이 수동 실행만(dry 기본 true)** — `LEGAL_CONTACT_EMAIL`이 공개
  페이지에 실리는 주소라 임의로 정하지 않았다. **secret 등록 후 사용자가 직접 실행**할 것.
- **대시보드 니치 카드**(dashboard `ddae4b9`, 인계 5번): 게임별 코드 수·최종 확인일,
  플랜트로그 발행 건수, niche-sites 크론. 코드 개수가 튀면 오탐 복귀 신호.

### 검증 결과 (07-31 오전 실측)
- **요즘경제 DRAFT 30554457539**: 표절 게이트 **통과**(본문 2,560자·이미지 1장 임시저장).
  소스에서 에세이류 제외한 수정이 먹혔다 — 가중치 조정 불필요.
- **형수 균형 로테이션**: 07-30 TECH NEWS → 07-31 Tech 101로 서로 다른 카테고리 선택.
  다만 `blog_category` 기록이 07-30부터라 표본 2건.
- **niche-sites 크론**: 겜코드 07:58 KST 정상. plantlog 과거 실패 2건은 **인코딩 파손**이
  원인이었고 ASCII 재작성으로 이미 해결(현재 3종 전부 비ASCII 0바이트).
- **회사 PC 제약**: 프록시로 외부 fetch 불가(넷마블 API·WP 모두). `py -3` 사용.
  niche-sites는 이 PC에 없어서 `niche_sites_repo/`로 클론함.

### 다음 할 일
1. **겜코드 13:00 크론 후 두 허브 육안 확인** — 날짜 정상 표기·모바일 카드·가짜 코드 제거.
2. **'상태' 열 판정 기준 결정**(위 ⚠️) — 사용자 판단 대기.
3. **`LEGAL_CONTACT_EMAIL` secret 등록 → legal_pages 워크플로 dry=false 실행**(9/11 역산).
4. **인계 3번 사이트 꾸미기**(로고·파비콘·테마색) — WP 관리자 접근 필요해 이 PC에선 불가.
5. 세나키 실제 쿠폰 수집 여부 확인 — 이제 0건이라 공지·이벤트 메뉴에서 진짜가 잡히는지 관찰.

---

## 2026-07-31 (금) 새벽 — 집 PC 마감 → 다른 PC 인계

### 완료(전부 push)
- **겜코드 공략 트랙 신설**: `niche_sites/gamcode/guide_post.py`. 목록 API 본문이 200자 프리뷰라
  **상세 API**(`article/{id}?menuSeq=N`)로 전문 확보 후 생성. 표절 게이트(18자 겹침·3회 재생성).
  첫 발행 `/sololv-guide-sung-jinwoo-hunter-weapon-guide/`. 크론 KST 15:00.
- **★넷마블 게임 목록 API 발견**: `forum.netmarble.com/api/game/list?languageCd=ko_KR` → 21개 게임의
  gameId/forumId 전체 매핑. **잘못된 코드도 HTTP 200+빈 menuList**라 len()으로 판정해야 함.
  → **세나키 활성화**(skiagb/SevenKnightsidle, menu 69·70·115·120): 1,018글 수집·코드 11개·
  허브 `/sena-idle-coupon/` 발행. 확장 후보: 레이븐2(raven2/raven2), 세나 리버스(tskgb/sena_rebirth).
- **요즘경제 표절게이트 재수리**: 세법 용어 나열 오탐 차단(_FIXED_TERMS 16종 추가) + **산문 판정**
  (서술어·연결어미 없는 청크는 표절 아님) + 슬라이딩 1/2→1/3. 3종 시나리오 테스트 통과.
  추가로 **소스 선정에서 에세이·자기계발류 제외**(수치 없는 글은 재서술 불가 → 게이트 3연속 실패 원인).
- **형수 트랙 행(hang) 수리**: tech_post.yml에 timeout-minutes 30(없어서 기본 360분 → 07-30 19:33
  슬롯이 4시간 7분 소진 후 취소, Tech 101 발행 유실). 가이드 트랙 concurrency group 분리
  (`naver-tech-guide`) — 멈춘 뉴스 런이 가이드까지 막던 구조 해소.
- niche-sites 워크플로 3종 ASCII 재작성(한글 주석 인코딩 파손 2회 재발 차단)+timeout 30분.

### 검증 결과 (07-30 실측)
- **현지언니**: 아침브리핑·정부지원·고CPC(세금절세/부동산주거) 전부 **새 가운뎃점 카테고리 정상 배정**,
  "카테고리 옵션 못 찾음" 경고 0건. 청약은 신규 공고 0건으로 정당한 no-op.
- **형수**: 영어 카테고리 정상(Mobility/TECH NEWS/Troubleshooting). 가이드 트랙 재개 후 첫 스케줄
  발행 성공. 균형 로테이션 동작 확인되나 표본 1건(07-31 재확인 필요).
- **niche-sites**: dispatch 스모크 2건 success, 플랜트로그 이미지 4장 정상, 겜코드 누적 57개.
  첫 스케줄 런은 07-31 07:00(겜코드)·11:00(플랜트로그)·15:00(공략).

### 다음 PC에서 할 일 (우선순위)
1. **요즘경제 DRAFT 30554457539 결과 확인** — 소스 선정 수정 후 게이트 통과했는지.
   실패면 소스 3곳(메르·홍춘욱·빠숑) 중 데이터 분석글 비중 높은 곳 위주로 가중치 조정 검토.
2. **07-31 스케줄 런 재검증**: (1)형수 균형 로테이션이 Tech 101→BEST PICK→Mobile 순으로 채우는지
   (2)niche-sites 첫 스케줄 3종 성공 여부(신규 레포는 첫 크론이 안 도는 경우 있음).
3. **niche-sites 사이트 꾸미기**(사용자 요청): 로고·파비콘·테마 색상(플랜트로그 그린, 겜코드 다크).
4. **법적 페이지**(소개·개인정보처리방침·연락처) — 9/11 애드센스 신청 전 필수.
5. 대시보드에 겜코드·플랜트로그 발행 현황 카드 추가.
6. `poster/naver_blog.py` 본문 입력 구간 타임아웃·재시도 보강(행의 근본 지점).
7. 이력 미커밋 ::warning 오탐 보정(발행 0건 스킵·취소 시에도 울림).

### 참고
- niche-sites 상세 인계: **`niche_sites/HANDOFF.md`** (레포 parky091999-sudo/niche-sites, 비공개)
- WP 인증정보는 집 PC `C:/박관용/CLAUDE/config/wp_new_sites_credentials.txt` (레포 밖) —
  다른 PC에서는 wp-admin에서 앱 패스워드 재발급이 빠름

---

## 2026-07-30 (목) 심야2 — ★겜코드·플랜트로그 사이트 오픈

- **서브도메인 전략 확정(사용자)**: 비용 0으로 시작, 월 수익 확인되는 사이트부터 도메인 구매·301 이전.
  이름=미래 도메인 라벨: gamcode(겜코드)·plantlog(플랜트로그).
- **사이트 라이브**: https://gamcode.hyunjiunni.com / https://plantlog.hyunjiunni.com —
  EC2 기존 서버, tech 패턴 복제(Apache vhost+SSL+GeneratePress+RankMath). 인증정보=집 PC
  C:/박관용/CLAUDE/config/wp_new_sites_credentials.txt (로컬 전용).
- **게임 파이프 핵심 실증**: 넷마블 포럼 공개 JSON API(api/game/{게임}/official/forum/{포럼}/article/list)
  — requests만으로 본문 포함 전체 수신, 당일 쿠폰 실코드(FIREITUP 등) 추출 성공. SPA라
  HTML 크롤링은 불가(스크리닝 에이전트의 SSR 판단은 착시였음).
- **애드센스 신청 = 9/11 (사용자 지정일)**. hyunjiunni.com 루트 승인 → 서브 자동 커버.
- **다음(순서)**: ①겜코드: 쿠폰 크롤러 정식화(멀티게임)+허브글 템플릿(코드표·확인일·클릭복사)
  ②플랜트로그: 키워드 매트릭스+관리법 템플릿 ③법적 페이지·저자 스키마 ④하루 1~2건 램프업.

### (심야3 추가) niche-sites 자동화 v1 가동 — 레포 parky091999-sudo/niche-sites(비공개)
- 겜코드: 넷마블 포럼 API 수집 + LLM 검증게이트(후보 중 선택만, 생성 차단) + 허브글 upsert.
  첫 실발행: /sololv-coupon/ (오늘자 코드 3개·확인일·복사버튼). 크론 하루 3회(07/13/21시).
  테마=OceanWP(정보킹 미러). 세나키는 games.json에 api_forum 미확정 — probe 후 활성화.
- 플랜트로그: 식물15×주제5 매트릭스 순차 생성(벤치 botanical.house 구조·1인칭 경험 톤).
  첫 실발행: 몬스테라 키우기. 크론 매일 11시 1글. 테마=Kadence(차별화. 벤치 carrino는 유료라 대체).
- 퍼머링크 .htaccess 수동 생성으로 REST 404 수리. 시크릿 3종 등록 완료.
- 다음: 법적 페이지(소개·개인정보), 세나키 포럼 probe, 대표이미지, 내부링크, 대시보드 연동.

## 2026-07-30 (목) 심야 — ★새 WP 니치 확정: 게임 + 원예 (사용자 확정)

- **12니치 SERP 재스크리닝**(개인 점유율 하드 게이트 — 사용자 지시로 신설) 결과로 결정.
  전체 실측·순위 = osmu_pipeline/docs/STRATEGY_new_wp_blogs.md §7 (대개정 커밋 0ad0505).
- **① 게임**: 쿠폰(유입)+공략(체류)+에뮬 제휴(수익) 3층. 넷마블·중국계·해외겜만(카카오·NC 제외).
  벤치마크 정보킹. 차별화=매일 자동 갱신+코드별 확인일 표기(경쟁자 태만 실측).
- **② 원예**: 식물종×관리항목×증상 3축 롱테일. 벤치마크 botanical.house(WP+애드센스+쿠팡
  이중수익 현역). YMYL 무.
- **폐기·연기**: 신용카드 Red(기업 장악+발급 CPA는 등록 모집인 전용=개인 불법), 반려동물 탈락(5%),
  자동차 행정=12월 착수 예약(1월 자동차세 대목 직전 론칭), 보일러 에러코드=겨울 시즌 카드 후보.
- **다음**: 도메인 2개 확정(사용자 구매) → WP 스캐폴드(GeneratePress+RankMath+IndexNow) →
  게임: 넷마블 포럼 쿠폰 크롤러 / 원예: 키워드 매트릭스+관리법 템플릿 → 하루 1~2건 램프업 →
  8월 말 20~40글로 애드센스 신청(9/20 수익화 역산).

---

## 2026-07-30 (목) — 집 PC 세션(다른 PC 이어작업) ★먼저 읽을 것

### 완료·커밋(전부 push)
- **QC FAIL 수정**: info 보험글 표셀 리터럴 `**` 누출 → `_strip_markers` 게이트 강화(9950c97) + **네이버 self-heal 정식배선**(4acb21d, qc_naver_selfheal). 라이브글 수리·재발행 완료. 라이브스캔 도구 scripts/_scan_live_markdown.py.
- **청약 중복 4건 삭제**(판교TH212 3+한화포레나 1, delete_posts.py).
- **형수테크 9카테고리 균형 재설계 + 주간상한 제거**(89a076c): NEWS_BLOG_RECIPES로 블로그카테고리 직접 로테이션(굶주린 카테고리부터), history에 blog_category 저장. DRAFT검증(TECH NEWS부터 채움). → 다음 크론부터 Tech 101·TECH NEWS 포함 7개 균형발행.
- **현지 요즘경제 표절게이트 과차단 수정**(2b2971d): 세법 고정용어·수치 마스킹 후 산문복제만 검출. DRAFT 발행통과 확인.

### 스레드/영상/WP 통계 분석(수익화 방향)
- 스레드: 일상글(현지1014·유진1067뷰) ≫ 상품글(22-33뷰). 영상 IG릴스 ~120뷰(팔로워 2-8명, **팔로워무관 알고리즘**). Threads가 IG릴스 8배. 상품 텍스트글 폐지·경험형 전환 권장. (67k뷰 영상=바이럴 상방 존재)
- WP: hyunjiunni 324뷰·38글, tech 223뷰·22글(신생 색인초기, 정상). 애드센스는 색인성숙 후. 고CPC(양도세·청약글) 우위.

### ★새 WP 블로그 2개 + 영어 애드센스 리서치(10에이전트) — 사용자 대기
- **전체 리포트 = osmu_pipeline(비공개)/docs/STRATEGY_new_wp_blogs.md** (공개레포 노출방지). 메모리 [[project_new_wp_blogs]].
- **새 WP: ①자동차=확정** (gov파이프 재활용·eulbblue동형·리콜API자동) **②사용자결정 대기: 신용카드 vs 반려동물**.
- **영어=원안 NO-GO / 재설계시 조건부GO**(12-18개월·SW/SaaS·전글사람검수·별도도메인, hyunjiunni에 영어섹션 절대금지). 지금 주력 아님.
- **다음**: ②니치 확정 → 자동차+②WP MVP 착수. 9/20 수익화 역산상 **애드센스 승인 히스토리(3-6개월) 때문에 지금 착수가 정확한 타이밍**.

### 미해결/대기
- 수익화 시점 7/31→**9/20경 연기**(실업급여 10차까지). 7-8주 여유=애드센스 색인성숙·계정성숙에 활용.
- 7/31 실무조치(쿠팡격리·AFID위생·인기글숨김·형수뉴스이미지 저작권·재발행이미지)는 9/20로 함께 순연 가능.

---

## 2026-07-29 (수) 심야 — 집 PC 세션 3부 (형수 대개편 + 카테고리 정비)

### 형수의테크공장 전면 개편 (사용자: "지금은 실험 단계 — 통계 판단은 표본 쌓인 뒤")
- **글 형식 5종 균등 로테이션**(pick/compare/top5/explain/breaking) — 벤치마킹 7블로그 실측으로
  형식 자체 개편: breaking=팩트체크·판정형(순수 속보 폐지), compare=승자판정형, top5 신설.
  fmt 이력로 형식별 성과 추적 → 표본 쌓이면(3~4주) 성적표로 비중 결정. 분석서는 세션 로그 참조.
  ※로테이션 index+1 방식의 중복 슬롯 고착 버그 수리(발행 횟수 기반으로).
- **영어 카테고리 확정**: BEST PICK(대표)/REVIEW(Mobile·PC·Laptop·Home Tech·Mobility)/
  How-To(Troubleshooting·AI Lab·Tech 101)/TECH NEWS. 내부 한글 주제명→발행 시 번역(CAT_BLOG_ALIAS).
- **가이드 트랙 재개**(05:11) — DRAFT 30455935734 통과. 프로필: 별명 테크형수·소개글·아이콘 적용.
- 현지언니 카테고리 개편 반영: 쉼표→가운뎃점 4개 + 아침증시 브리핑/쉽게보는 요즘경제 매칭.
- 벤치마킹 발견: 상위 테크 블로그 쿠팡 직링크 0(네이버쇼핑만) — 7/31 형수 배선은 쇼핑커넥트 중심.

### 검증 완료 (전 트랙 DRAFT green)
- 요즘경제 3차: 표절 게이트 통과(겹침 되먹임 효과)+연도 2026 강제. **첫 실발행 7/30 10:00.**
- 아침 브리핑: 가운데정렬·12소제목 성공. **첫 실발행 7/30 06:20.**
- info 3차: '함께 보면 좋은 글' 제목 생존(7/7) — URL 단락 Backspace 수리 확정.
- 댓글 스티커 첫 실전은 7/30 10:30 크론부터(오늘 21:30분은 워크플로 등록 직후라 미발화).

### 7/30 확인 사항
1. 첫 발행 4건(가이드 05:11→브리핑 06:20→정부지원 09:11→요즘경제 10:00)의
   '카테고리 선택:' 로그 — 새 카테고리에 실제로 꽂히는지.
2. 댓글 스티커 첫 실전(10:30) 등록 위치·모양 육안 확인.
3. 애드포스트·쿠팡·세시간전 = **7/31 일괄**(실업급여 종료 다음날 — 수급기간 소득활동 리스크 차단).

---

## 2026-07-29 (수) 밤 — 집 PC 세션 2부 (실험 2종 + 어드바이저 반영)

### 신규 트랙 2종 가동 (사용자 승인: 둘 다 현지언니, +2건/일)
- **실험A 아침 증시 브리핑** (`morning_brief_post`, 평일 06:20): 무지개로그 포맷 그대로 —
  Yahoo 무키 13지표(수치 결정론)+LLM 해석, 짧은 호흡(줄 중앙값 15자), ✅마커, 🔺🔻 등락,
  전체 가운데정렬. 글자색(상승빨강/하락파랑)·섹션 배너는 2단계. 분석서: docs/BENCHMARK_무지개로그.md
- **실험B 요즘경제 쉽게보기** (`econ_digest_post`, 매일 10:00): 메르(ranto28)·홍춘욱(hong8706)·
  빠숑(ppassong) RSS 소싱 → 팩트만 추출 → 현지언니화. 15자+ 원문 겹침 하드 게이트.
- ⚠️사용자: 블로그 관리에서 카테고리 2개 생성 — '아침 증시 브리핑', '요즘경제 쉽게보기'
  (없으면 기본 카테고리로 발행됨).

### 어드바이저 글별 분석 (에이전트, 원본 스크래치패드 advisor_deep_*.json)
- 현지: 트래픽 64~72%가 **네이버 메인 피드**(검색 아님). 세금절세 31뷰/글 최고(자녀장려금
  497뷰 지속형), 정부지원 지속형, 청약줍줍=시한부 고회전, **주식 ETF 9뷰/글 최하·당일 소멸**.
- 형수: 순수 검색형. 모델명 리뷰 롱테일(xq2566x 11일), 루머 뉴스 당일 소멸.
- **반영(9cc7cc5)**: 주식 종목분석 주2회·ETF 주1회로 축소 / info 세금절세 핸디캡 36h 가중 /
  형수 FMT_ROTATION breaking 1/6로 축소.

### 수리·기능 (커밋 로그 참조)
- ★생 URL 단락 제거 Backspace가 직전 단락 병합·삭제 → '함께 보면 좋은 글' 제목이 매 발행
  소실되던 근본 원인(6515ff8). 링크카드 정렬 Ctrl+E 폴백 3/3 동작, 표 데이터 셀 조건부
  가운데정렬 동작 확인(DRAFT 30451790276). 3차 DRAFT 30453842521로 제목 소실 재검증 중.
- 노매새드 게이트 require_numbers 파라미터화(info 번호 폐기 정합, gov 유지).
- 댓글 스티커 자동응답(comment_reply.yml, 하루 3회) — DRY_RUN 통과, 첫 실전 21:30 KST.
- 유진 '전부 렌더' 버튼 재고상한(30) 우회(dashboard 0ae4b19) — 21건 배치 렌더 가동.
- 청약 분양 홈페이지 평면도·배치도 수집(3단 필터+Gemini Vision)+타입 요약카드(528ec35).

---

## 2026-07-29 (수) 밤 — 집 PC 세션

### 🔴 청약 네이버 중복 발행 사고 — 규명·수리 완료, 삭제만 남음
- **원인 사슬**: ①7/26 이후 이력 미커밋(git add 회귀) → 7/27~28 크론이 같은 공고 재선정 →
  **판교TH212 4중 발행**. ②recover_history.py가 dict 이력(청약 네이버=공고번호 키)을 list로
  덮어씀 → 중복 가드(`key in history`) 무력화 + 발행 후 이력 저장 크래시 → 7/29
  **한화포레나 안산고잔2차 2중 발행** (런 2개 연속 failure의 정체).
- **수리(0247037 푸시됨)**: 이력 dict 복원(+판교·한화 항목), _load_history dict 가드
  (아니면 발행 전 중단), recover_history dict 파일 스킵.
- **⚠️ 사용자 수동 삭제 대상(중복 5건, 각 첫 글만 남김)**:
  판교TH212 → 224359666782 · 224360566554 · 224360817175 삭제 (224359424525 유지)
  한화포레나 안산고잔2차 → 224362015621 삭제 (224361773528 유지)

### 정보성(info) 글 구조 개선 4종 (7dad183) — 사용자 지시
① 소제목 번호(01~05) 폐기 ② '신청 방법' 섹션 존재 이유 검증(자명하면 실전 선택 정보로 교체)
③ '함께 보면 좋은 글' 소제목 매칭 수리(ZWSP 정규화 — 매 발행 스킵되던 것) + 진단 덤프
④ 링크카드 가운데정렬 수리(변환 대기+Ctrl+E 폴백) / 표 데이터 셀 조건부 가운데정렬
(비교값 표=전부 20자 이하만 가운데, 설명형 좌정렬 유지)
- **DRAFT 실측**: run 30450162612 (금융재테크, draft). 로그 확인 포인트: '함께 보면' 매칭,
  링크카드 N/N, 표 데이터 셀 정렬 판정, 소제목 번호 부재.

### 기타
- 총괄 대시보드 /queue에 EC2 렌더완료 재고 병합 표시(99bea79, osmu-dashboard) — 표시 전용.
- patent_1004 블로그(증시 브리핑, 가운데정렬·짧은 호흡) 구조 분석 진행 — 벤치마크 후보.
- 카페 등업글 초안 10개: 집 PC `C:\박관용\CLAUDE\카페_등업글_초안.md` (로컬 전용).

---

## 2026-07-29 (수) 저녁 — 회사 PC 마감 → 집 PC 인계 (★여기부터 이어서)

> 앞부분(낮 세션: 이력유실·유진영상·SE ONE 표·WP수리)은 아래에 이어짐. 저녁에 추가된 것:

### A. 유진 전용 랜딩 배포 완료 (jiniee.hyunjiunni.com)
- **왜**: 인스타 릴스 본문 링크 불가 → 프로필/댓글 → 랜딩 → 상품 동선. 현지(hyunjissi)와 계정·도메인 완전 분리.
- **새 public 레포 `parky091999-sudo/jiniee_landing`** 생성·Pages 활성. `http://jiniee.hyunjiunni.com` **라이브 확인**.
  가비아 DNS CNAME(jiniee→parky091999-sudo.github.io.) 추가 완료·전파됨. **⏳HTTPS 인증서 발급 대기**(GitHub 자동,
  발급되면 `gh api -X PUT repos/parky091999-sudo/jiniee_landing/pages -F https_enforced=true`).
- 빌더 `osmu_pipeline/scripts/build_jiniee_landing.py [--publish]`, 발행 시 `stock_publisher`가 자동 갱신·푸시.
  상품 URL은 candidate_pool 이름매칭(유진 이력엔 URL 없음). 검색URL·inpock·미매칭은 '준비중' 안전처리.
- 7/31 딥링크 교체: `data/jiniee_landing_links.json`에 {상품키: 파트너스URL} 넣고 재빌드.

### B. 네이버 카페 학습(블로그 SEO·수익) — 결과는 §10·메모리에 반영, 원문은 미커밋
- 카페 3곳 학습(블로그연구소 lablog 30089790 + WP수익형 wphomepage 29524583, **주임 등급 승급해 고급게시판까지**).
  총 381글·댓글 5,913 분석. **원문·분석 .md는 세션 스크래치패드에만**(등급게이트 콘텐츠라 public 레포 커밋 안 함).
- **핵심 결론 → `docs/WRITING_SYSTEM.md §10`에 일반화 반영(커밋됨 40fbb06)**: ①AI글 누락은 카더라 종결(누락=중복·무검증대량·상업성 3축, AI여부 아님) ②최대 레드라인=대량아이디 찍어내기→3계정 문체·구조·발행패턴 차별화 필수 ③멀티계정 3개 정상·IP분리 불필요(GH랜덤IP) ④발행후 잦은수정=누락(우리 재생성은 발행前이라 안전) ⑤쿠팡 등 수익링크 지수안정계정 격리·내돈내산 혼재금지·공정위 필수 ⑥정보요약 이탈→경험결합 피벗, 유진 국룰형 수혜, AI인용/메이트 편입이 다음 축.
- **윗등급(책임=고급정보20·고급연구242, 수석=비밀정보34) 제목 확인함**: '테스트해봤다/내부데이터/실험후기'류 고품질. 특히 수석에 "지수낮은 블로그로 쿠팡파트너스 하는법"·"쿠팡 링크삽입 실험후기" 있어 **7/31 쿠팡 리스크 직답**. → **책임 수동등업(정보글5+연구글5)이 자동등업(댓글300+방문50)보다 쉽고 규정준수. 봇 farming은 거절함(네이버 매크로조작 제재·D-2 계정리스크).**

### 🔜 집 PC에서 이어서 할 일 (우선순위)
1. **[사용자 대기] 카페 책임등업용 정보글 5 + 연구글 5 초안** — "글쓰기는 나중에" 하기로. 소재: 오늘 뚫은 SE ONE 표
   셀병합·색상(주임 초안 이미 scratchpad `카페_정보글_초안.md`에 1개), 자동화 트러블슈팅, 이미지 저작권 안전 등.
2. **7/31 수익화 D-2 실무 조치 5개**(전략은 이미 검증, 아래는 전술):
   - 🚩쿠팡 링크 격리 규칙(수익링크+내돈내산 혼재금지·신규저지수 계정 미투입·공정위 일괄) — coupang/osmu 배선에 반영
   - 🚩쿠팡 AFID 위생·도용 모니터링(위반메일→AFID교체 플레이북). 딥링크 빈값→수수료0 리스크와 함께.
   - 모바일 '인기글 숨김'을 현지·형수 네이버 계정 설정(효자키워드 카피 방어) — 수동 or Playwright
   - 형수테크 대표이미지 저작권: `generator/tech_image.py` 뉴스 og:image 1순위가 10년 합의금 리스크. **조치 시 자동화 안 깨짐**(AI대표이미지·쇼핑실사·무사진 폴백 존재). 사용자 판단 대기.
   - 재발행(키워드 로테이션) 시 이미지 재생성되는지 확인(중복이미지=누락).
3. **HTTPS 강제**(유진 랜딩, 인증서 발급 후 자동).
4. **파트너스 API 키** 확보되면 딥링크 배선(유진 landing_links·현지 랜딩 72건·스레드 댓글).

### 🔴 중대 사고 규명·수정 — 발행 이력 3일 유실 (내가 만든 회귀)
- **증상**: gov/info/청약 네이버가 07-26 이후 이력 0건 → '발행 침묵'으로 보였음.
- **실제**: **매일 정상 발행되고 있었고 이력만 커밋 안 됨**(07-28 gov 224360377846 · info 224360925112 로그 확인).
- **근본원인**: 07-27에 내가 QC 로그를 커밋 대상에 추가하며 `git add A B C 2>/dev/null || true` 형태가 됨.
  **git add는 한 pathspec이라도 없으면 전체가 실패**하는데, `qc_fail.jsonl`은 FAIL 0건이면 파일이 생성되지
  않음 → 매 실행 add 통째 실패 → 스테이징 0 → 커밋 없음 → `Everything up-to-date`. `|| true`가 은폐.
  (로컬 재현 검증 완료)
- **2차 피해**: info가 07-27~28 **4슬롯 전부 '부동산,주거'** 발행. `_pick_least_recent_category`가
  이력을 보고 고르는데 이력이 비어 매번 같은 카테고리를 '가장 오래됨'으로 판정.
- **조치**: ①워크플로 5종 개별 add 루프로 수정(`e427fe8`) ②유실 이력 14건 복구(`2ea7353`,
  `scripts/recover_history.py` — GH 로그에서 URL 추출, 멱등) ③이력 미커밋 시 `::warning::` 감시 가드(`0039dc2`).
- **로테이션 정상화 확인**: 시뮬레이션 결과 다음 크론은 금융재테크(07-26) 선택 — 편중 해소.

### 🔴 유진 영상 3일 미발행 — 해결
- **원인**: EC2 osmu 레포가 옛 커밋(`46090af`)에 고착 — 크론의 `git pull -q`가 조용히 실패해
  집 PC에서 승인한 30건이 EC2에 반영 안 됨(EC2는 5건짜리 구버전 승인 파일 → 전량 '미승인 스킵').
- **조치**: EC2 fetch·pull 복구 → 승인 35/35 반영 → **즉시 발행 성공**(간편 휴대용 블렌더,
  릴스 17899066590548516 + 스레드 17893528659575161). 타 레포 4개 점검 정상.

### ★SE ONE 표 조작 돌파 — '헤드리스 열삭제 불가'는 오진이었음
- 사용자 지적("셀 선택하면 삭제·병합 메뉴 뜨는데 왜 못 하나")으로 재조사 → **접근 경로 오류**였음.
  기존 5종 시도는 전부 컨트롤바 '열 선택' 클릭 경로였고 그 상태에선 컨텍스트 메뉴가 화면 밖
  (-713,-557)에 렌더돼 클릭 불가. **사람처럼 드래그로 셀을 선택하면 메뉴가 정상 좌표(979,408)로 들어옴.**
- **셀 병합 성공**(`579f2b0`): probe 실측 colspan `1/1/1` → `3`. `_merge_row_cells()` 구현,
  `_insert_faq_table`이 열삭제 대신 병합으로 1열 박스 생성.
- **셀 배경색 성공**(`a1cafb4`, `367f6a5`): 표 툴바에 정렬/너비/**셀배경색**/테두리/**테이블스타일** 전부 존재.
  ★`cell-background-color`(글자 배경색 `background-color`와 다름), 스와치는 **`data-color`** 속성
  (`data-value` 아님 — 첫 시도 실패 원인). E2E 실측: 헤더 3칸 `#b0f1ff` 정확 적용, 내용 밀림 없음.
  팔레트 파스텔 11종 확보. `TABLE_COLORS` 상수·`NAVER_TABLE_HEADER_COLOR`로 제어(기본 활성).
- **FAQ 박스**: 포스트잇 인용구 경로도 병행 구현(`f37ac64`, `NAVER_FAQ_BOX=1` 기본).

### WP 기존글 결함 수리 (감사 후속)
- **현지 WP 반말 24글 수리**(`0530a2a` 등): 원인=오해카드 `hj-myth-q`가 따옴표 없는 단정형 → 인용부호로 감쌈.
  물음표형·프롬프트 예시 인용문은 보호(감사 오탐 3건). 멱등 확인. `scripts/wp_repair_live.py`.
- **테크 WP 이미지 0장 14글 백필**(`--images`): `_inject_body_images` 재사용, 각 2장 섹션 일러스트.
- **재감사 결과**: 반말 16→2(잔여는 오탐), 테크 이미지 19→0.
- **감사 스크립트 이식성 수정**(`4735c1d`): 집 PC 절대경로 하드코딩 → 상대경로. cp949 콘솔 대응.
  ⚠️`AUDIT_INSECURE_SSL=1`(사내 프록시용 TLS 우회)를 내가 임의 도입했음 — 기본 꺼짐, 필요 없으면 제거 권장.

### 🔜 다음 세션
1. **DRAFT 검증 결과 확인**: run 30421229469(info 금융재테크 DRAFT) — FAQ 박스 병합+색이 실제
   발행 경로에서 동작하는지. 로그에서 `셀 병합 0행: 성공` / `셀 배경색 … 적용` 확인.
2. **info 로테이션 실측**: 다음 스케줄 실행이 금융재테크를 고르는지(편중 해소 확인).
3. 이력 감시 가드가 정상 동작하는지(Actions UI에 warning 안 뜨면 정상).

---

## 2026-07-29 (수) 아침 — 집 PC 마감 → 회사 PC 인계

### 이번 세션 완료 (전부 origin 푸시 완료)

**1. 발행글 전수 품질 감사** — 상세 리포트: [`docs/AUDIT_2026-07-29.md`](AUDIT_2026-07-29.md) ★먼저 읽을 것
사용자 지시가 "삭제가 목적이 아니라 왜 그런 글이 생성됐는지 파악하고 나머지 글도 점검"이었음.
5채널(네이버 2 + WP 2 + Blogger 1) 결정론 검출기로 감사 → 결함 유형별 원인→조치 매핑 완료.

**2. 감사에서 드러난 근본 결함 수리**
| 결함 | 원인 | 조치 |
|---|---|---|
| 현지언니 WP 30글 중 16글 반말 | **WP 트랙엔 반말 게이트가 아예 없었음** + 오해/사실 카드의 '오해:' 문장을 따옴표 없이 단정형으로 씀 | `deep_content` 검증에 `find_banmal_sentences` 하드게이트 + 오해 문장 인용형 강제 |
| 테크 WP 19/19글 본문 이미지 0장 | LLM이 순수 텍스트 HTML만 생성 — **삽입 배선 자체가 부재** | `_inject_body_images`로 h2 섹션에 무텍스트 일러스트 2장 주입 |
| 청약 글 3건 반말 | "현지언니 판단: **해볼 만하다**" 라벨이 프롬프트에 반말로 하드코딩 | "해볼 만해요/조건부 추천이에요/보류가 나아요"로 교체 |
| 커버리지 리뷰 크론 실패 | Gemini 503(일시 과부하) 단발 | 지수 백오프 3회 재시도 |

**3. 감사 도구 상시화** — 발행 후 재감사용
```bash
cd hyunji_unni_blog
python scripts/naver_audit.py   # 네이버 2블로그
python scripts/wp_audit.py      # WP 2 + Blogger(소유)
```

### 회사 PC 1순위
1. **`docs/AUDIT_2026-07-29.md` §3** — 심층가이드 잔여 9건(FAQ 상단 + 소제목 ①②③ 결함) 처리 결정.
   권고: 트랙 수리 실물 검증 → 삭제 후 같은 주제 재발행(주제 풀에 남아 있음).
2. **심층가이드 크론 재개 판단** — 현재 `.github/workflows/tech_guide_post.yml` schedule 주석 처리로
   정지 중. DRAFT/dry 1회 실물 확인 후 주석 해제.
3. **오늘 발행분 재감사** — 위 스크립트로 반말·본문 이미지 결함 0 확인.

### 사용자 액션 (Claude 대행 불가)
- ⚠️ **유진 영상 재고 30건 승인**: 대시보드 http://127.0.0.1:8899/jnstock (집 PC에서만 접속).
  7/26부터 매일 "미승인 스킵"으로 발행 중단 중. 승인 즉시 다음 슬롯부터 재개.
  (7/28에 검수 카드에 쿠팡 상품 링크 표시 추가해둠)
- 7/26자 레몬청 **릴스**는 인스타 앱에서 수동 삭제 필요(API 미지원). 스레드는 삭제 완료.

### 진행 중이던 작업 (다음 세션에서 이어서)
- **심층가이드 트랙 전면 수리**: ✅ 완료·푸시됨(`0468ba6`, WRITING_SYSTEM §7 0-t).
  결함 5종(목차 서식 불일치 / FAQ 상단 배치 / 소제목 번호 / 번호 리스트 계층 붕괴 / 구조 부정합)
  전부 수리. **단 크론은 여전히 정지 상태** — DRAFT/dry 1회 실물 확인 후 사람이 재개 결정할 것.
- **FAQ 1열 표(노매새드식)**: SE ONE 표 조작이 라이브에서 전부 폴백 + 빈 표 잔존 사고 →
  현재 기본 OFF(`NAVER_FAQ_TABLE=1`일 때만 시도). DRAFT로 표 조작 검증 후 재활성화.

### 7/31(금) 수익화 디데이 — 준비 필요
사용자: 쿠팡 파트너스 가입 → API 키 발급 → .env·GH Secrets 등록
Claude: ①soyu 전 글 data-slot 링크 일괄 교체 ②hyunji-zip 랜딩 70여건 교체 ③유진 stock meta
deeplink 배선 ④coupang_matcher 가동. **교체 스크립트를 미리 만들어두면 디데이 30분 컷**.
